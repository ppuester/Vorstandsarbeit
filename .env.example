# Payload CMS Konfiguration
# Kopiere diese Datei zu .env und fülle die Werte aus

# Payload Secret - WICHTIG: Generiere einen sicheren zufälligen String
# Du kannst einen generieren mit: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-here

# Datenbank URL
# Für lokale Entwicklung mit MongoDB:
# DATABASE_URL=mongodb://localhost:27017/msc-lennetal
# Für MongoDB Atlas oder andere Cloud-Datenbanken:
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/database-name
# Alternative Umgebungsvariablen (werden automatisch erkannt):
# MONGODB_URI=mongodb://localhost:27017/msc-lennetal
# MONGODB_URL=mongodb://localhost:27017/msc-lennetal
DATABASE_URL=mongodb://localhost:27017/msc-lennetal

# Server URL
# Für lokale Entwicklung:
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Für Production (wird automatisch von Vercel gesetzt):
# NEXT_PUBLIC_SERVER_URL=https://your-domain.com

# Website Name (für SEO und Meta-Tags)
NEXT_PUBLIC_SITE_NAME=Your Website Name

# Preview Secret - Für Draft Preview Funktionalität
# Generiere einen sicheren zufälligen String: openssl rand -base64 32
PREVIEW_SECRET=your-preview-secret-here

# Cron Secret - Für geplante Jobs (z.B. Scheduled Publishing)
# Generiere einen sicheren zufälligen String: openssl rand -base64 32
# Wird nur benötigt wenn du geplante Jobs verwendest
CRON_SECRET=your-cron-secret-here

# Vercel Blob Storage (Optional - nur für Production auf Vercel)
# Wird automatisch von Vercel gesetzt wenn Blob Storage aktiviert ist
# BLOB_READ_WRITE_TOKEN=

# Vercel Project URL (Optional - wird automatisch von Vercel gesetzt)
# VERCEL_PROJECT_PRODUCTION_URL=

# Node Options (Optional - für Performance)
NODE_OPTIONS=--no-deprecation
